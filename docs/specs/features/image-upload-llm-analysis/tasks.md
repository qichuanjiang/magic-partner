# 任务拆解：image-upload-llm-analysis

## 1. 开发实现

- [ ] T1：后端新增 `POST /api/images/analyze` 路由与请求解析（`UploadFile` + 可选 `prompt`）。
- [ ] T2：实现后端图片校验逻辑（MIME、大小上限、空文件校验）。
- [ ] T3：封装模型调用模块（输入图片 + prompt，输出标准结果 DTO）。
- [ ] T4：实现后端错误码映射（超时、上游异常、非法请求）。
- [ ] T5：前端新增上传组件（选择图片、预览、提交按钮、加载状态）。
- [ ] T6：前端新增分析结果展示区（摘要、标签、置信度、请求 ID）。
- [ ] T7：前端新增错误提示映射（按错误码展示对应文案）。
- [ ] T8：更新 API 类型定义，确保前后端字段一致。
- [ ] T9：新增环境变量配置说明（模型 API Key、超时配置）。

## 2. 质量保障

- [ ] Q1：完成 `test-plan.md` 中全部 P0/P1 用例验证。
- [ ] Q2：完成前端构建验证：`cd frontend && npm run build`。
- [ ] Q3：完成后端手工接口验证（成功/失败场景至少各 1 条证据）。
- [ ] Q4：回归验证 `GET /api/health` 与 `GET /api/greeting`。
- [ ] Q5：检查日志字段完整性（`request_id`、error code、latency）。

## 3. 发布准备

- [ ] R1：在 PR 中引用规格文档与测试证据。
- [ ] R2：补充 README 或部署文档中的环境变量说明。
- [ ] R3：准备回滚步骤（前后端版本回退、功能入口下线）。

## 负责人

- 后端：<owner>
- 前端：<owner>
- 测试：<owner>

## 依赖项

- Dep-1：可用的大模型图片分析 API 凭证。
- Dep-2：测试样本图片与基础夹具文件。
